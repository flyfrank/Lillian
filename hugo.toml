baseURL = "https://lillian.mpoom.cn/"
languageCode = "zh-cn"
defaultContentLanguage = "zh-cn"
title = "半度温差"
enableRobotsTXT = true
enableGitInfo = false
theme = "hugo-book"

# Book 主题配置
disablePathToLower = true

# 禁用 taxonomies（Book 主题推荐）
disableKinds = ["taxonomy", "term"]

[pagination]
pageSize = 10

# 将现有 static 目录作为静态资源目录使用
staticDir = ["static"]

# Markdown 渲染配置
[markup]
[markup.goldmark.renderer]
  unsafe = true

[markup.tableOfContents]
  startLevel = 1
  endLevel = 6

[params]
subtitle = "相拥而立,各自成诗。📖"
description = "关于《半度温差》:爱与差异的哲学"
author = "半度温差"

# Book 主题参数
BookTheme = "auto"
BookToC = true
BookSearch = true
BookComments = false
BookDateFormat = "2006年01月02日"

[outputs]
home = ["HTML"]
page = ["HTML"]
section = ["HTML"]

[menu]
  [[menu.after]]
  name = "首页"
  url = "/"
  weight = 10
  
  [[menu.after]]
  name = "文章"
  url = "/posts/"
  weight = 20
  
  [[menu.after]]
  name = "关于"
  url = "/about/"
  weight = 30