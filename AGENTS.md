# AGENTS 使用指南（半度温差）

本指南面向智能代理，规范在本项目中的工作方式与约束，确保所有变更与 Hugo + Book 主题的标准行为一致，且安全、可验证、可回滚。

## 项目概览
- 框架与主题：Hugo + Book 主题
- 发布目录：docs/（通过 Hugo 构建生成）
- 站点名称：半度温差
- Slogan：相拥而立，各自成诗。📖
- 自定义域名：lillian.mpoom.cn

## 关键文件
- 站点配置：[hugo.toml](file:///Users/huyaoke/Documents/github/Lillian/hugo.toml)
- 首页内容：[content/_index.md](file:///Users/huyaoke/Documents/github/Lillian/content/_index.md)
- 关于页面：[content/about/_index.md](file:///Users/huyaoke/Documents/github/Lillian/content/about/_index.md)
- 文章列表（时间线）：[content/posts/_index.md](file:///Users/huyaoke/Documents/github/Lillian/content/posts/_index.md)
- 示例文章（春节）：[spring-festival-2024](file:///Users/huyaoke/Documents/github/Lillian/content/posts/spring-festival-2024/index.md)
- GitHub Actions（若存在）：[.github/workflows](file:///Users/huyaoke/Documents/github/Lillian/.github/workflows)

## 内容结构与规则
- 使用 Hugo 标准分区：
  - 文章位于 `content/posts/...`，每篇为叶子 bundle（目录内的 `index.md`）
  - 列表页使用 `content/posts/_index.md` 承载分区的标题与简介
  - 首页与关于页分别为 `content/_index.md`、`content/about/_index.md`
- 旧链接兼容：
  - 列表页设置 `aliases: ["/events/"]`
  - 单篇文章在 front matter 添加 `aliases: ["/events/<slug>/"]` 保留旧地址
- 字段约定：
  - 标准字段：`title`、`date`、`tags`、`description`、`cover`
  - 业务分类（可选）：`eventType: traditional|birthday|anniversary`（不影响主题布局）

## 变更原则
- 遵循主题与 Hugo 的标准行为：不添加自定义代码、不覆写主题布局，除非用户明确要求
- 优先编辑现有文件，避免引入不必要的新文件与目录
- 严禁引入或暴露任何密钥、凭据或隐私数据
- 保持链接兼容与可回滚：修改前后均应确保 `/posts/` 与旧 `/events/` 路由可访问
- 不进行实际提交，除非用户明确要求；以补丁形式呈现变更并进行本地验证

## 常用操作流程
- 新建文章
  - 命令：`hugo new posts/my-post.md`
  - 建议：使用简洁中文标题、补充 `description` 与 `tags`
  - 图片与资源：放置在 `static/assets/images/...`（构建时自动复制到发布目录）
- 修改站点文案与信息
  - 标题与副标题：在 [hugo.toml](file:///Users/huyaoke/Documents/github/Lillian/hugo.toml) 的 `title`、`[params].subtitle`、`[params].description` 修改
  - 首页导语：编辑 [content/_index.md](file:///Users/huyaoke/Documents/github/Lillian/content/_index.md)
  - 关于页面：编辑 [content/about/_index.md](file:///Users/huyaoke/Documents/github/Lillian/content/about/_index.md)
  - 菜单导航：在 `hugo.toml` 的 `[menu.main]` 更新链接与顺序
- 路由与兼容
  - 列表页：`/posts/`（菜单指向该地址）
  - 旧地址兼容：使用 `aliases` 保留 `/events/` 与旧文章路径

## 构建与预览
- 本地开发预览：

```bash
hugo server -D -p 8080
```

- 生成发布产物：

```bash
hugo -d docs
```

- 静态预览（可选）：

```bash
npx --yes serve -s docs -l 8080
```

## 验证清单
- 构建成功且无报错
- 列表页 `/posts/` 展示文章，标题可点击进入详情
- 旧地址 `/events/` 与旧文章链接仍可访问（通过 `aliases`）
- 首页文案、关于页展示符合期望，度数符号等特殊字符正常渲染

## 清理与忽略
- Hugo 构建缓存：`/resources/_gen/...` 可以删除并在 `.gitignore` 中忽略（构建时自动重建）
- 不再使用的目录：`content/events/` 已移除（迁移到 `posts`），避免与标准分区冲突

## 安全与质量
- 不引入外部脚本、短代码或自定义布局，除非用户要求
- 图片路径统一为 `/assets/images/...`，为图片添加 `alt` 文本提升可访问性
- 文案与标题保持一致口径（中文、Slogan 与站点名统一）

## 常见问题
- 首页不显示文章列表：这是主题默认行为，列表页位于 `/posts/`，从首页导航进入
- 数学或特殊字符渲染：不启用数学渲染时，请直接使用 Unicode 符号（如 `0.5°`）
- 文章不可点击：确保使用主题默认的 `posts` 布局，不在页面中引入自定义包裹或拦截样式

